# Qesto JavaScript SDK (Public API v1)

Minimal **JavaScript** client for integrators. This is intentional platform surface — not Python.

Runtime product truth remains in:
- `functions/api/` (Hono Workers)
- `migrations/` + `schema.sql` (D1)
- `contracts/openapi-v3.json` (generated drift-checked spec)
