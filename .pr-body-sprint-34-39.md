## Summary
- **Sprints 34-35 (v2.3):** GDPR account deletion, live aggregate sentiment for presenters, AI recap provenance on exports, EU/compliance KB, admin engagement CSV, Zoom OAuth skeleton, SOC2 P4/P5 updates, and deploy fix for `deliverMarketingWebhook` import.
- **Sprints 36-39 (v2.4):** Team white-label branding (join + exports), PWA manifest/service worker + offline join cache, touch-friendly CSS, Salesforce/Notion/LDAP integration skeletons, HR webhook templates, bracket tournament REST API, facilitator AI coaching endpoint, agent grounding via `DECISIONS_VECTORIZE`, and `export.pdf` (print-ready HTML).

## Test plan
- [ ] `npm install && npm test && npm run typecheck`
- [ ] `node scripts/check-compliance-claims.mjs`
- [ ] `DELETE /api/users/me/gdpr-delete` (authenticated, staging)
- [ ] Presenter mood badge on open questions with `SENTIMENT_ENABLED=true`
- [ ] `GET /api/admin/engagement/export.csv` (admin)
- [ ] `PATCH /api/teams/:id` with `branding` (Team plan)
- [ ] Join via `/j/:code` — branding CSS vars applied
- [ ] `POST /api/tournaments/sessions/:id/bracket/seed` + list matches
- [ ] `POST /api/sessions/:id/coaching` after session close
- [ ] `GET /api/agent/grounding?q=...`
- [ ] `GET /api/sessions/:id/export.pdf` (Team plan, closed session)
- [ ] PWA: manifest loads; service worker registers in production build

## Notes
- Zoom/Salesforce/Notion/LDAP OAuth token exchange is skeleton-only (501/503 until secrets + full flows).
- PDF export is browser print-ready HTML, not a binary PDF renderer.
