# v2.5.0 — Enterprise Integrations + Platform API

**Target:** 2026-12-09 (Sprints 40–44)

## Highlights

- Zoom, Salesforce, and Notion OAuth completion
- Organization hierarchy (`/api/organizations`)
- Public API v1 + team API keys
- PWA v2 (offline votes, push scaffold)
- Admin engagement summary by energizer kind
- Multi-turn AI facilitator coaching
- Custom domain mapping for white-label join (`CNAME → join.qesto.cc`)

## Upgrade notes

- Set `OAUTH_TOKEN_MEK` before enabling new integrations in production.
- Create API keys via `POST /api/api-keys` (Team plan).

## Verification

```bash
npm run typecheck
npm test
npm run check:compliance-claims
```
