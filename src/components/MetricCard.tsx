/**
 * @deprecated Import from `../ui/components` — this file re-exports the unified MetricCard (DS Day 1–30).
 */
export { MetricCard } from '../ui/components'
export type { MetricTrend } from '../ui/components'
