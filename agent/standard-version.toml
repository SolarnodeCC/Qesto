standard = "jankurai"
standard_version = "0.9.0"
auditor_version = "1.6.10"
# Supply-chain pin (issue #613): the auditor is vendored via the
# `jankurai-workspace` dependency in package.json and locked in package-lock.json
# to a full commit SHA — not the mutable `v1.6.10` tag. CI builds the CLI from that
# locked workspace with `cargo install --locked`, so this SHA is the single source
# of truth for which auditor gates the repo. Keep in sync with package-lock.json.
auditor_source = "github:neverhuman/jankurai"
auditor_commit = "3c804453e6c7a6e0e4028d95cc3bccea467277ef"
schema_version = "1"
paper_edition = "2026-05-12"
target_stack = "cloudflare-workers-typescript-react"
target_stack_id = "cloudflare-workers-typescript-react"
published = "2026-06-06"
