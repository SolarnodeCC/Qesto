# CODEOWNERS — review routing for Qesto
#
# NOTE (advisory until enforced): this file routes review requests and marks
# ownership, but it only BLOCKS a merge once branch protection on `main` enables
# "Require review from Code Owners". Branch protection is not currently enabled,
# so today these entries are advisory (auto-request reviewers) rather than a gate.
# See knowledge-base/operations/security/GITHUB_INFRA_AUDIT_2026-07.md.

# Default owner for everything in the repo.
*                                   @SolarnodeCC

# Security-sensitive surfaces — edge runtime correctness, auth, multi-tenant
# isolation, CI/CD, and security docs. Kept explicit so ownership is unambiguous
# if additional collaborators are added later.
/.github/                           @SolarnodeCC
/.github/workflows/                 @SolarnodeCC
/functions/api/                     @SolarnodeCC
/worker/                            @SolarnodeCC
/schema.sql                         @SolarnodeCC
/ops/ci/                            @SolarnodeCC
/knowledge-base/operations/security/ @SolarnodeCC
