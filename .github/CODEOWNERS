# CODEOWNERS — review routing for Qesto
#
# NOTE (DD-31): the previous version of this comment claimed branch protection
# was not enabled. That is stale — the GitHub API reports `main` as
# "protected": true. What remains unverified from inside the repository is WHICH
# rules are configured; the branch-protection ruleset is not readable here.
#
# These entries BLOCK a merge only if the ruleset enables "Require review from
# Code Owners"; otherwise they auto-request reviewers. Confirm the ruleset in
# Settings -> Branches and record it alongside the other dashboard-only
# configuration (DD-20), so the repository stops disagreeing with reality.
# See knowledge-base/quality/audits/DUE_DILIGENCE_2026-09-15.md.

# Default owner for everything in the repo.
*                                   @SolarnodeCC

# Security-sensitive surfaces — edge runtime correctness, auth, multi-tenant
# isolation, CI/CD, and security docs. Kept explicit so ownership is unambiguous
# if additional collaborators are added later.
/.github/                           @SolarnodeCC
/.github/workflows/                 @SolarnodeCC
/functions/api/                     @SolarnodeCC
/worker/                            @SolarnodeCC
/schema.sql                         @SolarnodeCC
/ops/ci/                            @SolarnodeCC
/knowledge-base/operations/security/ @SolarnodeCC
